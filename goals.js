document.addEventListener('DOMContentLoaded',()=>{const goals=window.mkGoals,esc=window.safeText;if(document.querySelector('#goalsGrid'))document.querySelector('#goalsGrid').innerHTML=goals.map((g,i)=>`<article><span>0${i+1}</span><h2>${esc(g.title)}</h2><p>${esc(goalPreview(g))}</p>${i===1?'<a class="text-link" href="doel-2.html">Lees meer over dit doel →</a>':''}</article>`).join('');if(document.querySelector('#goal2Full')){const g=goals[1];document.querySelector('#goal2Full').innerHTML=`<div><p class="eyebrow">DOEL 02</p><h1>${esc(g.title).replace(' ','<br><em>')}</em></h1>${(g.body||'').split('\n').filter(Boolean).map((p,i)=>`<p class="${i===0?'lead':''}">${esc(p)}</p>`).join('')}</div><aside class="story-image">${g.image?`<img src="${g.image}" alt="${esc(g.title)}">`:'<><span>AFBEELDING</span><strong>Hier kan een foto<br>van het zendingswerk<br>of een logo komen.</strong><small>Te vervangen via Beheer → Doelen.</small></>'}</aside>`;}});
